/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["lh3.googleusercontent.com", "tenor.com"],
		formats: ["image/avif", "image/webp"],
	},
};

module.exports = nextConfig 
